/**
 * Shared tax constants for the planning services (deduped from npvService and
 * capexProjectionService — CR-023).
 */

/** Fallback marginal tax rate when an owner hasn't configured one (Swiss average), %. */
export const DEFAULT_MARGINAL_TAX_RATE_PCT = 25;
