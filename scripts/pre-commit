#!/usr/bin/env bash
# Git pre-commit hook — runs guardrail enforcement
# Install: cp scripts/pre-commit .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit

# Run guardrails (errors block commit, warnings are informational)
"$(git rev-parse --show-toplevel)/scripts/guardrails.sh"
